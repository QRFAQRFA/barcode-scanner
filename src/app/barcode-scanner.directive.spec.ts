import { BarcodeScannerDirective } from './barcode-scanner.directive';

describe('BarcodeScannerDirective', () => {
  it('should create an instance', () => {
    const directive = new BarcodeScannerDirective();
    expect(directive).toBeTruthy();
  });
});
